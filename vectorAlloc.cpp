#include <iostream>
#include <vector>

using namespace std;

int main() {

    vector <int> scores;

    vector <char> vowels;

    scores.push_back(10);
    vowels.push_back(15);

    cout << "show Vector Values" << scores;
}